// Shown wherever a buyer's seller-contact reveal happens under
// PAYMENT_MODE=contact_only (see backend/src/lib/config.ts) - the buy page's
// inline reveal, and the transaction detail page's contact-only view.
// Placeholder copy: needs real legal/product review before this ships for
// real, but the app should never show the contact reveal without some
// version of this warning in place.
export const CONTACT_ONLY_DISCLAIMER =
  "GrabMySeats does not process payment or verify this transaction. Confirm the ticket and seller in person before paying them directly. We recommend meeting in a public place and verifying the ticket before payment.";
